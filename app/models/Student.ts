/** Basic student identity. Used to differentiate submissions. */
export interface Student {
	/** Unique student identifier. */
	id: string;

	/** Display name or full name of the student. */
	name: string;
}
